# Batalha Naval – Nível Novato (C)

Programa em C que posiciona dois navios em um tabuleiro 10x10, recebendo as coordenadas pelo usuário.

## Como compilar

No terminal, dentro da pasta do projeto:

```bash
gcc -o batalha-naval batalha-naval.c
